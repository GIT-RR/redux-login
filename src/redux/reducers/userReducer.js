const userReducer = (state = '', action) => {
    switch (action.type, payload) {
        case 'SET_EMAIL':
            return true;
        default:
            return state;
    }
}

export default userReducer;